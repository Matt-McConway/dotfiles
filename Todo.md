# Todo

- Installs
  - ~~Homebrew~~
    - check in main install script as most install scripts use homebrew
  - Probably a number of other things I am forgetting!

- Config script and my dotfiles
- zsh config, and a replace fn to update parts rather than overwrite

- Rewrite in Rust lol 🦀
