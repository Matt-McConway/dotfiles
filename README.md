# Dotfiles

My dotfiles and setup for dev environment, heavily inspired by ThePrimeagen's [dev](https://github.com/ThePrimeagen/dev) setup.

To run, simply clone, chmod +x the scripts and run `./install`. Install supports `--dry` for dry runs, and a fuzzy filter e.g. `./install aero` to match aerospace, filtering out all other installers.
